CREATE TABLE `capitalAllocations` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`timestamp` timestamp NOT NULL DEFAULT (now()),
	`totalCapital` decimal(18,2) NOT NULL,
	`activeTradingCapital` decimal(18,2) NOT NULL,
	`maintenanceMarginBuffer` decimal(18,2) NOT NULL,
	`drawdownProtectionReserve` decimal(18,2) NOT NULL,
	`opportunityCapital` decimal(18,2) NOT NULL,
	`activeTradingPercent` decimal(5,2) NOT NULL,
	`marginBufferPercent` decimal(5,2) NOT NULL,
	`drawdownReservePercent` decimal(5,2) NOT NULL,
	`opportunityPercent` decimal(5,2) NOT NULL,
	`strategyAllocations` json,
	`trigger` enum('scheduled','performance','drawdown','volatility','manual') NOT NULL,
	`reason` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `capitalAllocations_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `paperTransactions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`type` enum('DEPOSIT','WITHDRAWAL','TRADE_PROFIT','TRADE_LOSS','COMMISSION','WALLET_RESET','ADJUSTMENT','POSITION_OPEN','POSITION_CLOSE') NOT NULL,
	`amount` decimal(18,2) NOT NULL,
	`balanceBefore` decimal(18,2) NOT NULL,
	`balanceAfter` decimal(18,2) NOT NULL,
	`relatedOrderId` varchar(100),
	`relatedPositionId` int,
	`description` text,
	`metadata` json,
	`timestamp` timestamp NOT NULL DEFAULT (now()),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `paperTransactions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `portfolioSnapshots` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`snapshotDate` timestamp NOT NULL,
	`totalEquity` decimal(18,2) NOT NULL,
	`cash` decimal(18,2) NOT NULL,
	`positionsValue` decimal(18,2) NOT NULL,
	`unrealizedPnL` decimal(18,2) NOT NULL,
	`realizedPnL` decimal(18,2) NOT NULL,
	`dailyReturn` decimal(10,6),
	`dailyPnL` decimal(18,2),
	`numberOfPositions` int NOT NULL,
	`positionDetails` json,
	`portfolioVaR95` decimal(18,2),
	`currentDrawdown` decimal(10,2),
	`sharpeRatio` decimal(10,4),
	`activeTradingCapital` decimal(18,2),
	`reserveCapital` decimal(18,2),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `portfolioSnapshots_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `positionRiskMetrics` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`positionId` int NOT NULL,
	`timestamp` timestamp NOT NULL DEFAULT (now()),
	`positionVaR95` decimal(18,2),
	`varContribution` decimal(18,2),
	`correlationWithPortfolio` decimal(5,4),
	`correlationWithOthers` json,
	`kellyOptimalSize` decimal(18,2),
	`currentSize` decimal(18,2),
	`sizeDeviation` decimal(10,2),
	`stopLossDistance` decimal(10,2),
	`takeProfitDistance` decimal(10,2),
	`riskRewardRatio` decimal(10,4),
	`holdingPeriod` int,
	`expectedHoldingPeriod` int,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `positionRiskMetrics_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `preTradeValidations` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`strategyId` int,
	`symbol` varchar(20) NOT NULL,
	`side` enum('long','short') NOT NULL,
	`requestedQuantity` decimal(18,8) NOT NULL,
	`requestedValue` decimal(18,2) NOT NULL,
	`currentPrice` decimal(18,8) NOT NULL,
	`passed` boolean NOT NULL,
	`overallRiskScore` decimal(5,2),
	`kellyOptimalSize` decimal(18,2),
	`kellyDeviation` decimal(10,2),
	`kellyPassed` boolean,
	`portfolioVaR` decimal(18,2),
	`positionVaR` decimal(18,2),
	`varLimit` decimal(18,2),
	`varUtilization` decimal(5,2),
	`varPassed` boolean,
	`circuitBreakerActive` boolean,
	`circuitBreakerReason` varchar(100),
	`circuitBreakerPassed` boolean,
	`availableBalance` decimal(18,2),
	`requiredMargin` decimal(18,2),
	`marginUtilization` decimal(5,2),
	`balancePassed` boolean,
	`currentPositions` int,
	`maxPositions` int,
	`positionLimitPassed` boolean,
	`rejectionReasons` json,
	`recommendedAction` text,
	`requiresApproval` boolean NOT NULL DEFAULT false,
	`approvedBy` int,
	`approvedAt` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `preTradeValidations_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `riskEvents` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`timestamp` timestamp NOT NULL DEFAULT (now()),
	`eventType` enum('drawdown_alert','var_breach','margin_warning','circuit_breaker_yellow','circuit_breaker_orange','circuit_breaker_red','circuit_breaker_emergency','position_size_violation','correlation_spike','volatility_spike','reserve_deployment','forced_liquidation') NOT NULL,
	`severity` enum('info','warning','critical','emergency') NOT NULL,
	`title` varchar(200) NOT NULL,
	`description` text,
	`portfolioValue` decimal(18,2),
	`drawdownPercent` decimal(10,2),
	`varBreach` decimal(18,2),
	`marginUtilization` decimal(5,2),
	`actionTaken` text,
	`positionsAffected` json,
	`capitalAdjustment` decimal(18,2),
	`resolved` boolean NOT NULL DEFAULT false,
	`resolvedAt` timestamp,
	`resolutionNotes` text,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `riskEvents_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `riskMetrics` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`timestamp` timestamp NOT NULL DEFAULT (now()),
	`portfolioValue` decimal(18,2) NOT NULL,
	`portfolioVaR95` decimal(18,2),
	`portfolioVaR99` decimal(18,2),
	`historicalVaR` decimal(18,2),
	`parametricVaR` decimal(18,2),
	`monteCarloVaR` decimal(18,2),
	`currentDrawdown` decimal(10,2),
	`maxDrawdown` decimal(10,2),
	`peakEquity` decimal(18,2),
	`drawdownDuration` int,
	`sharpeRatio30d` decimal(10,4),
	`sharpeRatio60d` decimal(10,4),
	`sharpeRatio90d` decimal(10,4),
	`sortinoRatio` decimal(10,4),
	`calmarRatio` decimal(10,4),
	`realizedVolatility` decimal(10,6),
	`impliedVolatility` decimal(10,6),
	`volatilityPercentile` int,
	`currentLeverage` decimal(10,4),
	`marginUtilization` decimal(5,2),
	`avgPositionCorrelation` decimal(5,4),
	`portfolioDiversification` decimal(5,4),
	`circuitBreakerLevel` enum('green','yellow','orange','red','emergency') NOT NULL DEFAULT 'green',
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	CONSTRAINT `riskMetrics_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `strategies` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`name` varchar(100) NOT NULL,
	`type` enum('momentum','mean_reversion','breakout','scalping','swing','arbitrage') NOT NULL,
	`status` enum('active','suspended','archived') NOT NULL DEFAULT 'active',
	`meanExcessReturn` decimal(10,6),
	`stdDeviation` decimal(10,6),
	`kellyFraction` decimal(10,6),
	`kellyMultiplier` decimal(5,4) NOT NULL DEFAULT '0.5000',
	`sharpeRatio` decimal(10,4),
	`sortinoRatio` decimal(10,4),
	`calmarRatio` decimal(10,4),
	`winRate` decimal(5,2),
	`profitFactor` decimal(10,4),
	`avgWin` decimal(18,2),
	`avgLoss` decimal(18,2),
	`maxDrawdown` decimal(10,2),
	`allocatedCapital` decimal(18,2) NOT NULL DEFAULT '0.00',
	`targetAllocation` decimal(10,2),
	`minAllocation` decimal(18,2),
	`maxAllocation` decimal(18,2),
	`maxPositionSize` decimal(10,2) NOT NULL DEFAULT '20.00',
	`maxCorrelation` decimal(5,4) NOT NULL DEFAULT '0.6000',
	`stopLossPercent` decimal(5,2) NOT NULL DEFAULT '5.00',
	`takeProfitPercent` decimal(5,2) NOT NULL DEFAULT '10.00',
	`lastRebalance` timestamp,
	`rebalanceFrequency` enum('daily','weekly','monthly') NOT NULL DEFAULT 'daily',
	`description` text,
	`config` json,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `strategies_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `strategyPositions` (
	`id` int AUTO_INCREMENT NOT NULL,
	`strategyId` int NOT NULL,
	`positionId` int NOT NULL,
	`isPaperTrading` boolean NOT NULL,
	`entryValue` decimal(18,2) NOT NULL,
	`currentValue` decimal(18,2),
	`pnl` decimal(18,2),
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `strategyPositions_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
CREATE TABLE `tradingStrategies` (
	`id` int AUTO_INCREMENT NOT NULL,
	`userId` int NOT NULL,
	`strategyName` varchar(100) NOT NULL,
	`strategyType` enum('scalping','day_trading','swing_trading','momentum','mean_reversion','breakout','trend_following','custom') NOT NULL,
	`description` text,
	`allocatedCapital` decimal(18,2) NOT NULL,
	`availableCapital` decimal(18,2) NOT NULL,
	`maxCapital` decimal(18,2) NOT NULL,
	`maxPositions` int NOT NULL DEFAULT 5,
	`maxDrawdown` decimal(10,2) NOT NULL DEFAULT '15.00',
	`maxDailyLoss` decimal(18,2) NOT NULL,
	`maxPositionSize` decimal(10,2) NOT NULL DEFAULT '20.00',
	`totalPnL` decimal(18,2) NOT NULL DEFAULT '0.00',
	`realizedPnL` decimal(18,2) NOT NULL DEFAULT '0.00',
	`unrealizedPnL` decimal(18,2) NOT NULL DEFAULT '0.00',
	`currentDrawdown` decimal(10,2) NOT NULL DEFAULT '0.00',
	`maxDrawdownReached` decimal(10,2) NOT NULL DEFAULT '0.00',
	`sharpeRatio` decimal(10,4),
	`sortinoRatio` decimal(10,4),
	`totalTrades` int NOT NULL DEFAULT 0,
	`winningTrades` int NOT NULL DEFAULT 0,
	`losingTrades` int NOT NULL DEFAULT 0,
	`winRate` decimal(5,2) NOT NULL DEFAULT '0.00',
	`avgWin` decimal(18,2),
	`avgLoss` decimal(18,2),
	`profitFactor` decimal(10,4),
	`performanceScore` decimal(5,2) NOT NULL DEFAULT '50.00',
	`isActive` boolean NOT NULL DEFAULT true,
	`isPaused` boolean NOT NULL DEFAULT false,
	`pauseReason` text,
	`lastTradeAt` timestamp,
	`createdAt` timestamp NOT NULL DEFAULT (now()),
	`updatedAt` timestamp NOT NULL DEFAULT (now()) ON UPDATE CURRENT_TIMESTAMP,
	CONSTRAINT `tradingStrategies_id` PRIMARY KEY(`id`)
);
--> statement-breakpoint
ALTER TABLE `users` MODIFY COLUMN `email` varchar(320) NOT NULL DEFAULT '';--> statement-breakpoint
ALTER TABLE `users` MODIFY COLUMN `loginMethod` varchar(64) NOT NULL DEFAULT '';--> statement-breakpoint
ALTER TABLE `paperPositions` ADD `exitReason` enum('manual','stop_loss','take_profit','liquidation','system');--> statement-breakpoint
ALTER TABLE `paperPositions` ADD `exitTime` timestamp;--> statement-breakpoint
ALTER TABLE `paperPositions` ADD `realizedPnl` decimal(18,8);--> statement-breakpoint
ALTER TABLE `positions` ADD `status` enum('open','closed') DEFAULT 'open' NOT NULL;--> statement-breakpoint
ALTER TABLE `positions` ADD `exitReason` enum('manual','stop_loss','take_profit','liquidation','system');--> statement-breakpoint
ALTER TABLE `positions` ADD `exitTime` timestamp;--> statement-breakpoint
ALTER TABLE `positions` ADD `realizedPnl` decimal(18,8);